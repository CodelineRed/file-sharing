{% if message.text|length > 0 %}
    <div class="row">
        <div class="col-12">
            <div class="alert alert-{{ message.style }} text-center" role="alert">
                {{ message.text }}
            </div>
        </div>
    </div>
{% endif %}
