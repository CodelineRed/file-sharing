<div class="modal fade" id="update-folder" tabindex="-1" role="dialog" aria-labelledby="update-folder-title" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg modal-xl" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="update-folder-title">{{ 'update-folder'|trans }}</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <label for="update-folder-name" class="col-form-label">{{ 'folder-name'|trans }}:</label>
                        <input type="text" class="form-control" id="update-folder-name" autocomplete="off" />
                    </div>
                    <div class="form-group mb-0">
                        <div class="mb-2">{{ 'access'|trans }}: </div>
                        <div class="btn-group-toggle btn-group-radio btn-group-access" data-toggle="buttons"></div>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">{{ 'close'|trans }}</button>
                <button type="button" class="btn btn-primary submit-form" disabled>{{ 'save'|trans }} <i class="fas fa-spin fa-sync-alt"></i></button>
            </div>
        </div>
    </div>
</div>
